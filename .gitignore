# dependencies — Vite bundles these into /dist at build time
node_modules/

# Vite build output (regenerated by `npm run build`; deploy this folder)
dist/

# design notes / scratch (safe to omit from deploys)
_design/
_design2/
prompts/
.spec_status.json

# temp capture/scratch files
_*.jpg
_*.png
_*.b64
_*.cjs

# Debug Lab (/lab.html) rendered captures — regenerated on demand
lab_captures/
